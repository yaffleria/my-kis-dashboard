# ============================================
# KIS Open API 설정 (한국투자증권)
# ============================================

KIS_ENV="prod"
KIS_ACCOUNTS='[]'
MANUAL_PORTFOLIO='[]'

# ============================================
# 데이터베이스 설정 (Supabase / Prisma)
# 가이드: 논풀링=POSTGRES_URL_NON_POOLING, 프리스마 주소=POSTGRES_PRISMA_URL
# ============================================

# 1. POSTGRES_URL (일반 다이렉트/풀링 주소 - 선택 사항)
POSTGRES_URL="postgres://postgres.xxxx:password@aws-0-ap-northeast-2.pooler.supabase.com:6543/postgres"

# 2. POSTGRES_PRISMA_URL (프리스마 런타임용 풀링 주소 - 6543 포트)
# 애플리케이션 실행 시 이 값을 필수로 사용합니다.
POSTGRES_PRISMA_URL="postgres://postgres.xxxx:password@aws-0-ap-northeast-2.pooler.supabase.com:6543/postgres?pgbouncer=true"

# 3. POSTGRES_URL_NON_POOLING (논 풀링/마이그레이션용 - 5432 포트)
# Prisma CLI(db push 등) 작업 시 이 값을 필수로 사용합니다.
POSTGRES_URL_NON_POOLING="postgres://postgres.xxxx:password@aws-0-ap-northeast-2.pooler.supabase.com:5432/postgres"

# 4. SUPABASE_CA_CERT (Supabase CA 인증서)
SUPABASE_CA_CERT="prod-ca-2021.crt 내용 복사"
